import React from "react"
import ReactDOM from "react-dom"

import './index.less'
import { Input, Button } from 'antd'


ReactDOM.render(
    <div>
        <h1 className="content">hello, world222333!</h1>
        <Input/>
        <Button type="primary">完成</Button>
    </div>,
    document.getElementById("root")
)
